#include<stdio.h>
#include<string.h>
int main()
{
    char s[100],s1[100];
    int i,n,result=0;
 
    scanf("%s%s",s,s1);
    n=strlen(s);
    for(i=0;i<n;i++){
    s[i] = tolower(s[i]);
    s1[i]= tolower(s1[i]);
    result=strcmp(s,s1);
    }
    printf("%d",result);
}
